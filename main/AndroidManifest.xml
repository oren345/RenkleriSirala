<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.renklerisirala">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".girissayfasi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".kolayortazor"></activity>
        <activity android:name=".levelkolay"></activity>
        <activity android:name=".oyun"></activity>
        <activity android:name=".levelorta"></activity>
        <activity android:name=".levelzor"></activity>
    </application>

</manifest>